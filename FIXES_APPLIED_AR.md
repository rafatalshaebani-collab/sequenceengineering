# الإصلاحات المطبقة

تم إصلاح مشكلة اختفاء نصوص مراحل دورة الحياة في صفحات الموقع عبر تصحيح JavaScript الخاص برسالة البريد الإلكتروني، وإضافة fallback يجعل عناصر `.stage` ظاهرة حتى عند تعطل JavaScript.

تم إصلاح بطاقة الموقع في صفحة التواصل بإزالة الارتفاع الأدنى غير الضروري من `.map-box`، واستبدال العنوان الوهمي `Google Maps Placeholder` بوصف فعلي لموقع مكتب Melbourne، مع إبقاء زر Google Maps.

تم حذف فقرات hero الفارغة من الصفحات الثانوية، وتوحيد الروابط الداخلية وcanonical وبيانات JSON-LD وsitemap على الروابط النظيفة دون امتداد `.html`.

تمت إضافة `_redirects` لتحويل الروابط القديمة ذات الامتداد `.html` إلى الروابط النظيفة بحالة 301، وإضافة صفحة Projects إلى التنقل الرئيسي والتذييل وSiteNavigationElement وsitemap.

تم اختبار HTML وJSON-LD وsitemap وredirects، كما تم فحص syntax لشفرة JavaScript التنفيذية في سبع صفحات. النتائج: **PASS**.

## النشر

ارفع محتويات هذا المجلد إلى الاستضافة كما هي، مع إبقاء ملف `_redirects` في الجذر. بعد النشر، افحص `/sitemap.xml` وجرّب `/data-centres` و`/contact`، ثم أرسل sitemap وأعد طلب الفهرسة للصفحات الأساسية من Google Search Console.
